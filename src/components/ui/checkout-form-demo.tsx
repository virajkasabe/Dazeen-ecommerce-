import CheckoutForm from "./checkout-form";

export default function CheckoutFormDemo() {
  return (
    <div className="flex items-center justify-center p-4 bg-transparent max-w-lg mx-auto">
      <CheckoutForm />
    </div>
  );
}
